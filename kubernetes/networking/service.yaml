apiVersion: v1
kind: Service
metadata:
  name: sms-svc
  namespace: comms
  labels: &SmsLabels
    app: sms
    tier: backend
    role: communication
    component: sms
spec:
  ports:
  - port: 4000
    name: http
    targetPort: 4000
  selector: *SmsLabels
