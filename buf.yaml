version: v1
breaking:
    use:
        - FILE
lint:
    use:
        - DEFAULT
    except:
        - ENUM_VALUE_PREFIX
        - ENUM_ZERO_VALUE_SUFFIX
        - ENUM_VALUE_UPPER_SNAKE_CASE

    ignore_only:
        PACKAGE_VERSION_SUFFIX:
            - app/messages/events/events.proto

    allow_comment_ignores: true
